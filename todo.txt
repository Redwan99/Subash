Subash — Pending Tasks
======================

1. [ ] Revert sidebar widths back to original, make center column full-width
       - Previously changed --sidebar-width from 260px→220px and --sidebar-right-width from 300px→260px in globals.css
       - Revert those back to 260px / 300px
       - Instead, the MAIN content area between sidebars should use full available width
       - Grid columns were also reduced (e.g. xl:grid-cols-4→3) in HomeFeedSection, EncyclopediaMatrix,
         EncyclopediaClient, creators pages — keep those changes since they make cards wider
       - Files: app/globals.css (CSS vars), components/layout/LayoutShell.tsx (margin classes)

2. [ ] Enrich encyclopedia with proper filtering (mood, weather, time, notes)
       - Filter bar stays on the LEFT panel inside the encyclopedia page (not the global sidebar)
       - Perfume grid should be full-width on the remaining page area
       - Current filters: Main Accords, Sort By (in EncyclopediaMatrix.tsx)
       - Add new filters: Mood (romantic, fresh, cozy, confident, mysterious),
         Weather (hot, cold, mild, humid, rainy), Time (morning, day, evening, night),
         Top/Middle/Base Notes, Gender, Price Range, Longevity, Sillage
       - Files: components/encyclopedia/EncyclopediaMatrix.tsx, components/perfume/EncyclopediaClient.tsx
       - DB fields available: accords, notes_top/mid/base, gender, season_tags, time_tags in prisma/schema.prisma

3. [ ] Replace all emojis with Lucide SVG icons across codebase
       - ~20+ files use emoji characters (🧴💐⚡👤🌸☀️🍂❄️🌅🌤️🌇🌙🔥📈⭐🏆💎💬❤️👍 etc.)
       - Replace with appropriate lucide-react icons (e.g. 🧴→Droplets, ⭐→Star, 🔥→Flame, ❄️→Snowflake,
         ☀️→Sun, 🌙→Moon, 💐→Flower2, ⚡→Zap, 👤→User, 💬→MessageCircle, ❤️→Heart, 👍→ThumbsUp,
         🏆→Trophy, 💎→Gem, 📈→TrendingUp, 🌸→Cherry/Flower, 🍂→Leaf, 🌅→Sunrise, 🌇→Sunset)
       - Key files: CommunityConsensus.tsx (SEASONS/TIMES arrays), ReviewForm.tsx (WEATHER/TIME chips),
         NotificationBellClient.tsx (ICON_FOR_TYPE), WardrobePanel.tsx (SHELVES), api/sidebar/route.ts,
         email.ts, EncyclopediaClient.tsx, ChatWidget.tsx, ClimatePerfumeSlider.tsx, ScentOfTheDay.tsx,
         TrendingPerfumes.tsx, creators pages, search/page.tsx, user/[id]/page.tsx, PerfumeHero.tsx
       - Server components (email.ts, api/sidebar/route.ts) that render to HTML strings can keep
         text-based icons or use inline SVG strings instead of React components

4. [ ] Fix UTF-8 encoding on all source files
       - PowerShell's -replace and [System.IO.File]::WriteAllText wrote some files in Windows-1252
       - Already fixed 5 files (ThemeToggle, decants/page, leaderboards/page, SmartSearch, DecantMarketClient)
       - Run a full scan: read all .tsx/.ts files with strict UTF-8 decoder, re-encode any that fail
       - Command: $utf8Strict = New-Object System.Text.UTF8Encoding($false, $true)
         then for each file try GetString($bytes) — if it throws, re-encode via 1252→UTF8

5. [ ] Fix ReviewForm — make it a horizontal, wider popup
       - Currently opens as max-w-lg (32rem/512px) narrow vertical modal in both:
         • ScentProfile.tsx (line ~380) — the main perfume page review popup
         • WriteReviewModal.tsx — standalone trigger version
       - Change to max-w-3xl or max-w-4xl (~48-56rem) for a wider horizontal layout
       - Remove overflow-y-auto / max-h scrollbar attachment — let it be a clean floating popup
       - Reorganize form sections side-by-side (e.g. sliders left, chips right) to use horizontal space
       - Keep mobile responsive (stack vertically on small screens)
       - Files: components/perfume/ScentProfile.tsx, components/perfume/WriteReviewModal.tsx,
         components/perfume/ReviewForm.tsx

6. [ ] Verify Docker build compiles clean
       - Run: docker build -t subash:test .
       - Must pass: next build (TypeScript + ESLint + static page gen)
       - Watch for: invalid UTF-8 errors, missing imports, type errors from icon changes
       - Previous build time: ~72s total (24s compile + 48s Docker overhead)
